const color = require("colors")

console.log("welcome to ACC");
// setTimeout(() => console.log("I am excited to teach".red), 1000);
console.log("Hope you are also excited");

setTimeout(() => {
    const user = { id: 1, }
    
    setTimeout(() => {
        const product = [{}, {}, {}];

        setTimeout(() => {
            const price = [];
        }, 1000);
    }, 500);

}, 1000);